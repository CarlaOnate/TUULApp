import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    textCenter: {
        textAlign: 'center'
    },
    fullWidth: {
        flexDirection: 'row',
        flexGrow: 2
    }
});
